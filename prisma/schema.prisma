// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Base schema restored from main.
// Phase 1 Marketing* models + reverse relations are in the following commit.
// See docs/specs/marketing-module-layout.md and artifacts/phase0-phase1/prisma/SCHEMA-MARKETING-PHASE1.md
