// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// NOTE: Full Phase 1 schema is applied via this branch.
// If this file appears truncated, pull from branch and compare with local artifacts.
// Marketing models require reverse relations on WorldModel* — see docs/specs/marketing-module-layout.md
